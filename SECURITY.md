# Kuaiz CMS Community 安全说明

## 支持范围

只有 `cms.kuaiz.net` 下载页明确列为“仍受支持”的正式版本属于安全维护范围。开发预览、
源码快照、第三方修改版和没有通过官网公钥指纹验证的安装文件，不作为正式发行版处理。

## 私下报告漏洞

请把复现步骤、影响版本、受影响功能和必要日志发送到 `support@kuaiz.net`，主题以
`[Kuaiz CMS Security]` 开头。不要在工单正文、截图或日志中发送管理员密码、安装码、
会话 Cookie、API Key、客户正文或真实个人信息；可以使用最小化的测试数据复现。

在收到确认前，请不要公开尚未修复的可利用细节。我们会先确认是否能复现和影响范围，
再给出修复、缓解或不受影响的结论。这个流程不是漏洞奖励计划，也不承诺固定奖金。

## 正式文件如何验证

正式安装器必须同时满足：

1. 从 `https://cms.kuaiz.net/download/` 进入下载；
2. 安装器内嵌发行公钥的 SHA-256 指纹与官网公布值完全一致；
3. 安装器在显示管理员表单前通过发行签名、模板哈希和全部载荷哈希校验；
4. 官网版本记录中的文件 SHA-256 与下载文件一致。

官网尚未公布生产公钥指纹时，表示公开发行尚未开始，不存在可被视为“正式版”的安装器。

## 安全更新原则

- 私钥不进入源码仓库、CI、下载服务器、总台 Web 进程或客户主机；
- 新版本先生成规范发行摘要，再由离线签名环境签名；
- 公钥轮换必须有旧钥交叉声明或明确的安全公告，不能静默替换官网指纹；
- 已确认高风险问题会暂停受影响版本下载，并给出升级或临时缓解办法；
- 安全修复不会要求客户把数据库、密码或模型密钥上传给快智。
